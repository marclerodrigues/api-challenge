require "rails_helper"

RSpec.describe DnsRecordsQuery, type: :query do
  describe ".all" do
    context "when included hostnames are present" do
      context "when only included are present" do
      end

      context "when excluded are present" do
      end
    end

    context "when included hostnames are not present" do
      context "when only included are not present" do

      end

      context "when excluded are not present" do
      end
    end

    context "when included and excluded hostnames are not present" do
    end
  end
end
